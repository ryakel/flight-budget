# Flight Budget Configuration
# Copy this file to .env and customize for your deployment

# App Settings
APP_PORT=8181
TIMEZONE=America/New_York

# FAA Aircraft Lookup Feature
# Enable the tail-lookup service for automatic FAA aircraft data verification
# When enabled:
# - Automatically verifies aircraft data from FAA registry during CSV import
# - Displays "FAA Verified" badges on verified aircraft
# - Uses lightweight SQLite-based service (~256MB memory)
# - No database setup required
ENABLE_FAA_LOOKUP=false

# Security Notes:
# - Never commit .env file to version control
# - Use environment-specific configurations for production deployments
